services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - facefusion-data:/data
    restart: unless-stopped

volumes:
  facefusion-data:
